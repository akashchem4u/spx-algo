# Signal Ablation Report

Status: pending runtime generation

This placeholder exists so the `Codex/` review channel always has a stable path
for the ablation artifact.

Current state:

- the ablation study UI is implemented in `app.py`
- the app writes a populated version of this file when the research ablation
  surface runs successfully
- until then, this file should be treated as a placeholder, not as model evidence

To replace this placeholder with a populated report:

1. run the app with live market-data access
2. open the `Research & Validation` tab
3. expand `Signal Ablation Study`
4. let the app complete the 2-year analysis and overwrite this file
